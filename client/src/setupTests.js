// Adds the toBeInTheDocument()/toHaveTextContent()/etc. matchers used across
// the test files below. Loaded automatically for every test via
// vite.config.js's test.setupFiles.
import "@testing-library/jest-dom";
